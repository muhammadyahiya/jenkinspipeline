pipeline {
      agent any
      stages {
         stage("Build") {
           steps {
              echo "checkout ocde from git"
              }
            }
            stage("Test"){
               steps{
                 echo "code testing has started"
                 echo "code testing finished"
          }
       }
      }
    }
